all:
  children:
    homelab:
      hosts:
        magi-02:
          # ansible_host: 90.90.90.90 # Default ip for root
          ansible_host: "{{ magi-02_ip }}"
        # server2:
        #   ansible_host: 192.168.1.101
